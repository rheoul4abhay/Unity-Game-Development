using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Health : MonoBehaviour
{
    public static float totalHealth = 1f; //1f so that we could sync it with the scale of the bar(0 to 1) to make it
    //change based on the totalHealth remaining.
}
